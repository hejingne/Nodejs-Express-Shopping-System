extends layout

block content
  h1= title

  ul
    each item in item_list
      li
        a(href=item.url) #{item.name}
        |  (designed by #{item.brand.name} - #{item.brand.country})
        |  [#{item.description}]
        |  [#{item.category}]

    else
      li There are no items.
